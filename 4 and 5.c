#include<stdio.h>
int main(){
    // char a = 'a';
    // printf("The size of character %c is %d \n",a , sizeof(a));

    // int b = 10;
    // printf("The size of real constant %d is %d \n",b , sizeof(b));

    // printf("The character is %c and after increment is %c \n",a,a+1);

    // int c = 10 ;
    // int d = 20 ;

    // printf("The num before is %d and then is %d \n",c,c+d-c);
    // printf("The num before is %d and then is %d \n",d,d+c-d);

    // int a = 241214;
    // int b = a / 10;
    // int c = b*10;
    // printf("%d",c);
    
    // int a , b , c ;
    // printf("Enter the number and the number you want to append in the last");
    // scanf("%d%d",&a,&b);
    
    // c = ((a/10)*10)+b;
    // printf("%d",c);

    char a = '+';
    printf("%d",a);
    
    return 0;
}